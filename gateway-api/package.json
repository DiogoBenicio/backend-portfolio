{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway — JWT Auth + Reverse Proxy | Node.js SOA",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fastify/cors": "^8.0.0",
    "@fastify/rate-limit": "^9.1.0",
    "axios": "^1.7.7",
    "fastify": "^4.28.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^20.16.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2"
  }
}
